import React from "react";

const NotFound = () => {
    return (
        <div className="not_found" style={{width: "100%", height: "500px", display: "flex", alignItems: "center"}}>
            <h2 style={{width: "100%", textAlign: "center", fontSize: "3rem", color: "#fff"}}>404 Error</h2>
        </div>
    );
};

export default NotFound;
